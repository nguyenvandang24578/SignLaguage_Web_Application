# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
env/

# OpenCode
.opencode/
opencode.json

# Environment & secrets (private repo — keep .env tracked for convenience)
# .env
# .env.local
# .env.*.local

# Chatbot data & databases
# Note: data directories are now tracked by Git
# Only ignore transient SQLite WAL files
*.db-shm
*.db-wal

# Accidental data copy inside src/ (should be at backend/ level only)
chatbot/backend/src/chroma_db/

# Model files (quá nặng cho Git)
chatbot/backend/models/
*.gguf

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db